name: Senior Developer Agent

on:
  schedule:
    # Runs daily at 1:00 PM UTC
    - cron: '0 13 * * *'
  workflow_dispatch:

jobs:
  run-senior-developer:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync

      - name: Run Senior Developer Agent
        env:
          PYTHONPATH: .
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          JULES_API_KEY: ${{ secrets.JULES_API_KEY }}
          GITHUB_OWNER: juninmd
          DEV_AGENT_ENABLED: true
        run: |
          uv run run-agent senior-developer

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: senior-developer-results
          path: logs/senior-developer-*.json
          retention-days: 30
